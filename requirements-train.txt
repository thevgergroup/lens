# Training dependencies for LENS forensic model
#
# Setup:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-train.txt
#
# Note: tensorflow_decision_forests is NOT installed here. It is a transitive
# dependency of tensorflowjs but has an irreconcilable protobuf conflict with
# TF 2.19.0 (gencode 6.x vs runtime 5.x). The training script mocks it out via
# sys.modules before import. tensorflow-hub is installed separately as a direct dep.

tensorflow==2.19.0
tensorflowjs==4.22.0
tensorflow-hub==0.16.1
numpy==2.1.3
scikit-learn==1.9.0
pillow==12.2.0
wandb==0.27.2
datasets>=3.0.0
